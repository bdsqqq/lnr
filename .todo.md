# lnr todo

task tracking per [ADR-0006](docs/adr/0006-todo-task-tracking.md).

- [x] 0001 - entity expansion per [ADR-0005](docs/adr/0005-entity-expansion-roadmap.md)
  - [x] 0002 - add Template entity support (high priority)
  - [x] 0003 - add Cycle entity support
  - [x] 0004 - add CustomView entity support
  - [x] 0005 - add Team read-only support --blocks 0003
  - [x] 0006 - add User read-only support
  - [x] 0007 - add ProjectUpdate scoped to project
  - [x] 0008 - add Notification read-only support
  - [x] 0009 - add Initiative read-only support
  - [x] 0010 - add InitiativeUpdate scoped to initiative --blocks 0009
  - [x] 0011 - add Roadmap read-only support
  - [x] 0012 - add RoadmapToProject via roadmap command --blocks 0011
  - [x] 0013 - add ProjectLabel scoped to project
  - [x] 0014 - add ProjectStatus scoped to project
  - [x] 0015 - add EntityExternalLink as --link flag
  - [x] 0016 - add ViewPreferences embedded in view command --blocks 0004
  - [x] 0017 - extend Reaction to all comment-like entities
  - [x] 0018 - add NotificationSubscription as --subscribe/--unsubscribe flags
  - [x] 0019 - add IssueBatch as `lnr issue batch` subcommand
  - [x] 0020 - add GitAutomationState support
  - [x] 0021 - add GitAutomationTargetBranch support
  - [x] 0022 - add AgentSession support (experimental)
  - [x] 0023 - add AgentActivity embedded in session --blocks 0022
- [x] 0024 - update entity-config.ts with all categorized entities --blocks 0001
- [x] 0025 - update introspect-linear.ts to fetch all supported entity types --blocks 0024
- [x] 0026 - graceful error handling for enterprise-only entities
- [x] 0027 - verify git automation API requirements (team context?)
- [x] 0028 - add --milestone flag to issue create/update
- [x] 0029 - scope milestones to project per [ADR-0004](docs/adr/0004-milestones-scoped-to-projects.md)
- [x] 0030 - remove standalone milestone command

## verification (thread T-019c1144-4c4d-73c8-9c41-93e55f73bc4b)
- [x] 0031 - run introspection against live Linear API to verify schema fetch
- [x] 0032 - spot-check CLI commands: lnr templates, lnr cycles, lnr views
- [x] 0033 - spot-check CLI commands: lnr notifications, lnr initiatives, lnr roadmaps
- [x] 0034 - spot-check CLI commands: lnr agent-sessions, lnr git-automations, lnr git-branches
- [x] 0035 - verify enterprise error handling on non-enterprise workspace (returns empty, no error)
- [x] 0036 - e2e tests split: e2e-readonly.test.ts (10 tests), e2e-mutations.test.ts (31 tests)

## bugs found during e2e
- [x] 0037 - project --subscribe fails: notificationSubscriptionTypes must be derived from schema, not hardcoded
- [x] 0038 - project --unsubscribe requires subscription id (UX: should auto-find user's subscription)

## entity-config v2 exploration (thread T-019c2e72-60b1-73a3-b52a-edcfff839e92)
investigating pattern problem: flag entities manually edited in generated files instead of codegen'd.
see [ADR-0007](docs/adr/0007-entity-config-v2-exploration.md).

- [x] 0039 - install arktype dependency
- [x] 0040 - create entity-schema.ts with arktype DSL
- [x] 0041 - create entity-definitions.ts with all entity definitions
- [x] 0042 - create ADR-0007 documenting exploration
- [x] 0043 - implement flag injection in generate-commands.ts --blocks 0040
- [x] 0044 - implement scoped entity injection --blocks 0043
- [x] 0045 - implement subcommand injection --blocks 0043
  - [x] 0045.1 - add ProjectMilestone subcommand entity for CRUD (project milestone) --blocks 0045
- [x] 0046 - regenerate all files and verify --blocks 0043,0044,0045
- [x] 0047 - remove manual edits from generated files --blocks 0046
  - issue config now uses generateFlagZodFields/generateScopedZodFields
  - all flag entities (Reaction, NotificationSubscription) injected from entity-definitions
  - subcommands (IssueBatch, ProjectMilestoneCRUD) generated from entity-definitions

### missing from regeneration (audit from 19cf9fd)
- [x] 0048 - issue --subscribe/--unsubscribe handler logic
  - issue removed from NotificationSubscription parents (uses different API)
  - handler uses subscribeToIssue/unsubscribeFromIssue (subscriberIds, not NotificationSubscription)
  - flags added inline in issueConfig, not via flag injection
- [x] 0049 - project --links uses getProjectExternalLinks (read-only)
  - added ProjectExternalLink as scoped entity in entity-definitions.ts
  - handler code in generateProjectShowHandler()
  - createEntityExternalLink still missing in core (future enhancement)

### 0043-0044 implementation notes (thread T-019c2edb-6bdb-7358-a556-fa0042d39805)
- generator now reads from entity-definitions.ts via helper functions
- generateFlagZodFields(), generateScopedZodFields() inject zod fields
- getInjectedImports() adds handler imports (createReaction, getProjectUpdates, etc.)
- getInjectedMutationFlags() extends inferOperation mutation list
- flag handler code added to generateProjectUpdateHandler() (react/subscribe)
- scoped handler code added to generateProjectShowHandler() (updates/labels/status)
- simplified output (console.log) instead of fancy table columns
- commented out EntityExternalLink (createEntityExternalLink missing in core)
- ProjectMilestone restored: uses listMilestones which exists in core

## zod → arktype migration
- [x] 0050 - migrate generated CLI schemas from zod to arktype
  - trpc v11 supports arktype via Standard Schema (co-authored by arktype, zod, valibot)
  - usage: `t.procedure.input(type({ name: "string" }))` — no adapter needed
  - benefits: unified validation library, ~100x faster validation, cleaner string-based syntax
  - scope:
    - [x] 0050.1 - update generate-commands.ts to emit arktype instead of zod
    - [x] 0050.2 - update graphqlTypeToZod → graphqlTypeToArktype in types.ts
    - [x] 0050.3 - update non-generated routers (auth, config, search, etc.)
    - [x] 0050.4 - remove zod dependency from cli package
    - [x] 0050.5 - verify trpc integration works with arktype schemas

## feature parity audit (pre-merge blockers)
codegen regressions found comparing main router files to generated output.
all fixes must go through codegen (entity-definitions.ts / generate-commands.ts), never hand-edit generated files.

### regressions from main
- [x] 0051 - issue --branch flag missing from codegen (main had it, hand-crafted/issue.ts has handler but nothing wires it)
- [x] 0052 - issue --pr flag missing from codegen (main had it, hand-crafted/issue.ts has handler but nothing wires it)
- [x] 0053 - issue --open flag defined in schema but handler never runs (main opened browser via spawn)
- [x] 0054 - projects --team/--status filters not passed to listProjects() in generated project.ts
- [x] 0055 - issue show no longer auto-fetches comments (main did, HEAD requires --comments flag) --blocks 0051

### missed migration
- [x] 0056 - template.ts still uses zod (all other generated files use arktype, task 0050.4 incorrectly marked done)

### new dead flags (never worked, not on main)
- [x] 0057 - issues --priority/--cycle list filters defined but not wired to core ListIssuesFilter
- [x] 0058 - issue --prioritySortOrder defined but not in mutationFlags or update handler

### issue show UX downgrades (fixed via output rendering architecture)
- [x] 0059 - output rendering architecture: renderers (detail, comments) + adapters (issue, project, label, doc)
- [x] 0060 - issue show: full description, parentId, updatedAt, url fields restored
- [x] 0061 - issue show: threaded comments via outputCommentThreads with chalk (was flat preview)
- [x] 0062 - issue show: graceful comment error handling (was silent)
- [x] 0063 - issue --branch uses issue.branchName (was handleBranch)
- [x] 0064 - issue --open says "opened" (was "opening")
- [x] 0065 - issue --comments uses outputCommentThreads (was flat list)
- [x] 0066 - project/label/doc show handlers use adapter + outputDetail (was inline console.log)
- [x] 0067 - extract comment rendering from output.ts into renderers/comments.ts

## property-based testing (thread T-019c3a42-917f-71b8-b4d2-f6889accd4ef)
test dispatch + payload wiring, not schema validation. requires DI refactor for stub client.

- [x] 0068 - export `inferOperation` + `mutationFlags` from generated/hand-crafted commands (or extract operationSpec)
- [x] 0069 - ~~dependency injection~~ replaced with bun mock.module() — no DI needed
- [x] 0070 - property A: operation dispatch determinism — `inferOperation` returns exactly one op for any valid input --blocks 0068
- [x] 0071 - property B: precedence under contradictory flags — delete/archive wins over update --blocks 0068
  - discovered: cycle --current has highest precedence (above new), view --preferences below delete
- [x] 0072 - property C: mutation flag completeness — every mutation flag triggers update, absence triggers read --blocks 0068
- [x] 0073 - property D: flag → payload effect — setting a flag changes the API payload (use mock.module)
  - tested: title, description, priority, state → updateIssue payload
- [x] 0074 - property E: resolver call discipline — resolver called iff flag present, at most once (use mock.module)
  - tested: state→getTeamStates, label→getTeamLabels, priority→priorityFromString, read→none
- [x] 0075 - property F: output-format equivalence — json/quiet/table agree on entity set (use mock.module)
  - tested: issue list json/quiet/table same entity count + identifiers
- [x] 0076 - support Linear OAuth tokens (`lin_oauth_*`) in client auth
  - [x] 0077 - add core tests for token-prefix auth option selection --blocks 0076
- [-] 0078 - investigate git automation e2e ci failure
  - [x] 0079 - stop swallowing git automation read errors in core --blocks 0078
- [x] 0080 - refresh live linear schema and upgrade sdk to 95.1.0
  - [x] 0081 - regenerate commands and reference, resolve compatibility breaks --blocks 0080
  - [x] 0082 - verify typechecks, safe tests, and binary build --blocks 0081
    - 72 core + 172 cli tests pass; read-only e2e agent sessions needed a 120s timeout (49s observed)
    - local nix-bun binary needed ad-hoc re-signing before macos would execute it
    - mutation e2e initially deferred; authorized sandbox verification completed in 0086
- [x] 0083 - fix pr 24 ci mock isolation and exclude live e2e from unit discovery
  - bun 1.4.2: 83 core + 172 isolated cli tests pass; client-first and dispatch-first orders pass
- [x] 0084 - preserve cycle and git automation creation errors with offline regression tests
- [x] 0085 - diagnose live cycle/git automation failures from surfaced errors in an authorized sandbox run
  - cycleCreate is deprecated upstream; new teams contain default git automation rules
- [x] 0086 - verify automatic-cycle and default-automation fixtures in sandbox ci
  - [run 35778328773](https://github.com/bdsqqq/lnr/actions/runs/35778328773): 255 unit, 10 read-only e2e, and 32 sandbox mutation tests pass
- [-] 0087 - reach full supported public api parity per [api update runbook](docs/updating-linear-api.md)
  - complete only when 0098 passes; intentional public exclusions and unverified capabilities remain gaps
  - [-] 0088 - capture the complete public operation/type surface with sdk version and schema provenance --blocks 0089,0096
    - complete graphql capture accepted at 2026-09-22T23:22:42.359Z; two matching passes over 1,208 types; non-graphql sdk/upload reconciliation remains
    - include root query/mutation types, arguments, deprecations, interfaces/unions, nested inputs, and referenced output types; reject partial fetches
    - reconcile public sdk api methods not represented by graphql introspection; do not count sdk utilities as server operations
  - [-] 0089 - produce an operation/field coverage inventory against actual cli and core wiring --blocks 0090,0091,0092,0093,0094,0095,0096
    - source inventory and 480 offline root witnesses exist; per-field bindings, scope/auth classification, and live evidence remain incomplete
    - record source, command/flag, resolver, payload or output mapping, test evidence, and implemented/missing/excluded/blocked/upstream-unavailable status
    - reconcile entity-config, entity-definitions, generator targets, and hand-written routers; categorization is not implementation evidence
    - split remaining operation gaps into bounded follow-up tasks before implementation; deprecated does not automatically mean unavailable
  - [ ] 0090 - close input/payload gaps in existing commands --blocks 0098
    - start with agent-session summary and document ownerId; audit every accepted flag for dispatch and api effect
    - test create/update differences, omitted/null/false/zero/empty-list semantics, enum validation, and argv parsing
  - [-] 0091 - implement missing operations on currently read-only or scoped entities --blocks 0098
    - derive operations from 0089, not the old read-only labels; cover both generated and hand-written commands
  - [-] 0092 - add deferred public entities and link/upload operations --blocks 0098
    - inventory external-link creation, attachments, favorites, and release-related capabilities; add entity/command ADRs before implementation
  - [-] 0093 - cover public admin, integration, webhook, membership, and plan-gated operations --blocks 0098
    - replace blanket exclusions with operation-level work; document required auth/scopes/plan and verify only in authorized environments
  - [-] 0094 - cover read arguments, pagination, relations, and output fields --blocks 0098
    - verify filters, ordering, cursor traversal, archived data, and full json output; a first-page list is not full read parity
  - [-] 0095 - verify operation errors and lifecycle behavior across the inventory --blocks 0098
    - distinguish api rejection, empty/missing results, and failed follow-up reads; do not convert exceptions into successful empty results
    - account for permissions, async creation, default entities, and safe cleanup; never retry an uncertain write automatically
  - [-] 0096 - add a replayable api drift and coverage gate --blocks 0098
    - detect added/removed/changed operations, arguments, fields, enums, nullability, and deprecations; fail on unclassified changes
    - retain explicit gaps in the report; prove checks catch missing dispatch, dropped payload fields, and unsupported operations
  - [-] 0097 - make live verification fail honestly and isolate sandbox ownership --blocks 0098
    - remove blanket e2e catches, fail explicit e2e runs without credentials, and assert persisted effects
    - scope cleanup to owned fixtures and prevent concurrent runs from deleting one another's data; keep module-level e2e out of unit discovery
    - first slice removes broad scavenging, requires explicit credentials, isolates child config, and serializes sandbox ci; persisted-delete assertions and interrupted-write recovery remain open
  - [ ] 0098 - verify and publish the parity report for a pinned sdk/schema snapshot --blocks 0087
    - zero unresolved supported-public-operation/input/output gaps, linked regression evidence, authorized live verification, reproducible generation, and passing head ci
    - report internal/upstream-unavailable capabilities separately; blocked verification cannot be reported as complete
