---
"@bdsqqq/lnr-core": minor
"@bdsqqq/lnr-cli": minor
---

add schema-validated graphql access with offline validation by default and explicit execution for both queries and mutations. preserve variables and partial data, reject internal and known-unavailable capabilities, and derive validation from the complete captured schema.
