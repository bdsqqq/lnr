---
"@bdsqqq/lnr-core": minor
"@bdsqqq/lnr-cli": minor
---

add local OAuth URL/PKCE helpers and explicit token, refresh, client-credentials and revocation requests. token output requires separate consent; no automatic retries or credential persistence.
