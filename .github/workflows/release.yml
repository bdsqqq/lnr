name: Release

on:
  push:
    branches: [main]
  workflow_dispatch:

concurrency: ${{ github.workflow }}-${{ github.ref }}

jobs:
  release:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      id-token: write # required for npm OIDC trusted publishing
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          fetch-tags: true
          ssh-key: ${{ secrets.DEPLOY_KEY }}
      - uses: oven-sh/setup-bun@v1
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          registry-url: 'https://registry.npmjs.org'

      - run: bun install

      # ensure npm 11.5.1+ for OIDC support
      - run: npm install -g npm@latest

      # generate changesets from conventional commits since last tag
      - name: Generate changesets from commits
        run: npx changeset-conventional-commits

      # check if there are changesets to process
      - name: Check for changesets
        id: changesets
        run: |
          if ls .changeset/*.md 2>/dev/null | grep -qv README.md; then
            echo "has_changesets=true" >> "$GITHUB_OUTPUT"
          else
            echo "has_changesets=false" >> "$GITHUB_OUTPUT"
          fi

      # version packages and update changelogs (commits directly to main)
      - name: Version packages
        if: steps.changesets.outputs.has_changesets == 'true'
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          npx changeset version
          git add -A
          git commit -m "chore(release): version packages"
          git push

      # publish to npm
      - name: Publish to npm
        if: steps.changesets.outputs.has_changesets == 'true'
        run: npm run release

      # get current version for github release (after versioning)
      - name: Check for missing GitHub release
        id: release-check
        run: |
          VERSION=$(node -p "require('./packages/cli/package.json').version")
          echo "version=$VERSION" >> "$GITHUB_OUTPUT"
          if gh release view "v$VERSION" &>/dev/null; then
            echo "exists=true" >> "$GITHUB_OUTPUT"
          else
            echo "exists=false" >> "$GITHUB_OUTPUT"
          fi
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      # build binaries for all platforms when release is missing
      - name: Build binaries
        if: steps.release-check.outputs.exists == 'false'
        run: bun run build:all

      # create github release with binaries
      - name: Create GitHub Release
        if: steps.release-check.outputs.exists == 'false'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          VERSION="${{ steps.release-check.outputs.version }}"
          gh release create "v$VERSION" dist/* \
            --title "v$VERSION" \
            --notes "See [CHANGELOG](https://github.com/bdsqqq/lnr/blob/main/packages/cli/CHANGELOG.md) for details."

      # update nix package hashes after release
      - name: Update nix package
        if: steps.release-check.outputs.exists == 'false'
        uses: cachix/install-nix-action@v27
      - name: Update nix hashes and commit
        if: steps.release-check.outputs.exists == 'false'
        run: |
          VERSION="${{ steps.release-check.outputs.version }}"
          ./scripts/update-nix-hashes.sh "$VERSION"
          git add nix/default.nix
          git commit -m "chore(nix): update to v$VERSION"
          git push
