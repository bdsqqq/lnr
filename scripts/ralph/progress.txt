# Ralph Progress Log
Started: 2026-01-30
Feature: entity expansion per ADR-0005

## Codebase Patterns
- entity config in packages/codegen/entity-config.ts controls extraction
- introspection in packages/codegen/introspect-linear.ts fetches types
- commands generated in packages/codegen/generate-commands.ts
- core logic in packages/core/, CLI presentation in packages/cli/
- scoped entities use flags on parent commands (ADR-0004 pattern)
- run `bun run check` before shipping
- templates accessed via team.templates() in Linear SDK
- read-only entities don't need full CRUD generator, hand-craft simpler commands

---

## 2026-01-30 - Add Template entity support
Thread: https://ampcode.com/threads/T-019c10f1-6955-7429-8eff-45f24c78e5fc
Task ID: 0002

**What was implemented:**
- Added Template to SUPPORTED_ENTITIES in entity-config.ts
- Added Template type to priorityTypes in introspect-linear.ts
- Ran introspection to fetch Template type from Linear API
- Created packages/core/src/templates.ts with listTemplates, getTemplate, findTemplateByName, getIssueTemplates
- Exported template functions from core index.ts
- Created packages/cli/src/generated/template.ts with `lnr templates` and `lnr template <name>` commands
- Wired up generatedTemplatesRouter in router/index.ts

**Files changed:**
- packages/codegen/entity-config.ts
- packages/codegen/introspect-linear.ts
- packages/codegen/schema.json (regenerated)
- packages/core/src/templates.ts (new)
- packages/core/src/index.ts
- packages/cli/src/generated/template.ts (new)
- packages/cli/src/router/index.ts

**Learnings:**
- Template is read-only in Linear API (no create/update/delete mutations)
- Templates scoped to teams via team.templates() connection
- For read-only entities, simpler hand-crafted CLI commands work better than full codegen

---

## 2026-01-30 - Add Cycle entity support
Thread: https://ampcode.com/threads/T-019c10f6-01f3-7113-a0c6-1f775409c22d
Task ID: 0003

**What was implemented:**
- Extended Cycle type with description, completedAt, progress fields
- Added CreateCycleInput, UpdateCycleInput types
- Added core functions: getCycle, getCycleById, createCycle, updateCycle, deleteCycle
- Exported new functions and types from core index.ts
- Upgraded router/cycles.ts with full CRUD pattern:
  - `lnr cycles --team <key>` - list cycles
  - `lnr cycle <name|number> --team <key>` - show specific cycle
  - `lnr cycle new --team <key> --starts-at <date> --ends-at <date>` - create
  - `lnr cycle <name> --team <key> --name <new-name>` - update
  - `lnr cycle <name> --team <key> --delete` - archive
  - `lnr cycle <name> --team <key> --current` - show active cycle
  - `lnr cycle <name> --team <key> --issues` - list cycle issues
- Updated contract tests for new cycle input schema

**Files changed:**
- packages/core/src/types.ts
- packages/core/src/cycles.ts
- packages/core/src/index.ts
- packages/cli/src/router/cycles.ts
- packages/cli/src/router/contract.test.ts

**Learnings:**
- Cycle is Tier 2 full CRUD (has createCycle, updateCycle, archiveCycle in Linear SDK)
- Cycles scoped to teams via team.cycles() connection
- Linear uses archiveCycle for deletion (not deleteCycle)
- Existing cycles.ts router was basic; upgraded to match doc.ts CRUD pattern

---

## 2026-01-30 - Add CustomView entity support
Thread: https://ampcode.com/threads/T-019c10fa-7fc0-728d-8bb3-57c81be9bc58
Task ID: 0004

**What was implemented:**
- Added CustomView to SUPPORTED_ENTITIES in entity-config.ts
- Added CustomView, CustomViewCreateInput, CustomViewUpdateInput to priorityTypes in introspect-linear.ts
- Added CustomView, CreateCustomViewInput, UpdateCustomViewInput types to core/types.ts
- Created packages/core/src/views.ts with listViews, getView, getViewById, createView, updateView, deleteView
- Exported view functions and types from core/index.ts
- Created packages/cli/src/router/views.ts with full CRUD pattern:
  - `lnr views` - list custom views (alias: `lnr v`)
  - `lnr view <name|id>` - show specific view
  - `lnr view new --name <name>` - create view
  - `lnr view <name> --name <new-name>` - update view
  - `lnr view <name> --delete` - delete view
- Wired up viewsRouter in router/index.ts

**Files changed:**
- packages/codegen/entity-config.ts
- packages/codegen/introspect-linear.ts
- packages/core/src/types.ts
- packages/core/src/views.ts (new)
- packages/core/src/index.ts
- packages/cli/src/router/views.ts (new)
- packages/cli/src/router/index.ts

**Learnings:**
- CustomView is Tier 2 full CRUD (has createCustomView, updateCustomView, deleteCustomView in Linear SDK)
- CustomViews are user-scoped (not team-scoped like Cycles)
- Linear uses deleteCustomView (not archiveCustomView like Cycles)
- filterData is a JSON object containing view filter configuration

---

## 2026-01-30 - Enhance Team entity CLI support
Thread: https://ampcode.com/threads/T-019c10fd-9020-775d-a058-745d20e6fa91
Task ID: 0005

**What was implemented:**
- Enhanced router/teams.ts with --verbose flag for extended columns
- Added --quiet flag to `lnr team <key>` command (already existed on `lnr teams`)
- Created reusable column definitions: teamColumns, verboseTeamColumns, memberColumns, verboseMemberColumns
- Refactored to use OutputOptions/getOutputFormat pattern (consistent with template.ts)
- Added --quiet support for members output
- Shows ID in verbose mode for both teams list and single team

**Files changed:**
- packages/cli/src/router/teams.ts

**Learnings:**
- Team was already Tier 1 read-only with working commands; just needed enhancement
- Existing teams.ts had good structure, only missing verbose mode and consistent output handling
- No core changes needed - Team type and functions were already complete

---
